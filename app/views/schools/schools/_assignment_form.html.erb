
<div class="card" data-my-nested-form-target="card" id="nestedform">
  <div class="row">
      <%= f.simple_fields_for :assignments do |a| %>
        <% if a.object.new_record? %>
          <div class="col">
            <%= a.input :teacher_message, label:"Message au remplaçant" %>
          </div>
          <div class="col">
            <%= a.input :area_message, label:"Message au gestionnaire" %>
          </div>
            <div class="col">
            <%= a.input :date, as: :date, :order => [:day, :month, :year], input_html: {value: Date.today} %>
            <%#= a.input :date, as: :date %>
            <%# <%= a.input :date, as: :date, data:{ provide:'datepicker' } %>
            <hr>
            </div>
            <div>
            <button type="button" class="btn btn-outline-primary" data-action="nested-form#remove">Supprimer</button>
            <%= a.hidden_field :_destroy %>
            </div>
        <% end %>
      <% end %>
  </div>
</div>
